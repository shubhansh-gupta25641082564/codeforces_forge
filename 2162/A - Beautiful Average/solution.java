import java.io.*;import java.util.*; public class Average {    public static void main(String[] args) throws Exception {        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));        StringBuilder out = new StringBuilder();        int t = Integer.parseInt(br.readLine().trim());        while (t-- > 0) {            int n = Integer.parseInt(br.readLine().trim());            StringTokenizer st = new StringTokenizer(br.readLine());            int[] a = new int[n];            for (int i = 0; i < n; i++) a[i] = Integer.parseInt(st.nextToken());            long[] p = new long[n + 1];            for (int i = 0; i < n; i++) p[i + 1] = p[i] + a[i];            long bestSum = a[0];            int bestLen = 1;            for (int i = 0; i < n; i++) {                for (int j = i; j < n; j++) {                    long s = p[j + 1] - p[i];                    int L = j - i + 1;                    if (s * bestLen > bestSum * L) {                        bestSum = s;                        bestLen = L;                    }                }            }            out.append(bestSum / bestLen).append('\n');        }        System.out.print(out.toString());    }}